# =============================================================================
# Template for ~/.claude/.env.local
# =============================================================================
# Copy to .env.local and fill in your real values:
#   cp .env.example .env.local
#
# The real .env.local is gitignored. This template (.env.example) is checked in
# so a fresh clone knows which env vars settings.json expects.

# PostHog analytics MCP. Get a key from your project at https://eu.posthog.com.
POSTHOG_API_KEY=phx_replace_me

# Add more secrets below as needed.
